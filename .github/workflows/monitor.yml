name: polling

on:
  schedule:
    - cron: '*/5 9-22 * * *'    # JST 18:00〜翌07:55
  workflow_dispatch:            # 手動実行用（任意）

jobs:
  monitor:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: pip install requests

      - name: Run script
        env:
          URL1: ${{ secrets.URL1 }}
          URL2: ${{ secrets.URL2 }}
        run: python main.py
